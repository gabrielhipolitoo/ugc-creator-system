describe("Login with invalid credentials", () => {
  it("Given I am on the login page", () => {
    cy.visit("/");
  });

  it("When I fill the email with teste@gemail.com", () => {
    
  });
  it(" And I fill the password with sdabf33223", () => {
    
  });
  it(" And I click the login button", () => {
    
  });
  it("Then I should see the error message Invalid credentials", () => {
    
  })
});
