// TODO: Fix import path for handlers and signIn. 
// If "@/auth" is incorrect, update to the correct module path.
// For example, if the correct path is "./auth", use the following:

import { handlers, signIn } from "@auth";
export const { GET, POST } = handlers;